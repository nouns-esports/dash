// @ts-nocheck

function createIntegration(input: {
    id: string;
}) {
    return input
}

createIntegration({
    id: "discord",
    name: "Discord",
    image: "",
    description: "Discord servers",
    config: z.object({})
})