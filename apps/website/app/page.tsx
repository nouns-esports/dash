// TODO: If the user is logged in, redirect to chat to select a community (if in multiple) or infer from referer or other data if possible

export default function Home() {
    return (
        <main className="flex h-screen w-screen items-center justify-center">
            Nothing to see here... yet
        </main>
    );
}
